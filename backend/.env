# Basic
PORT=4000
JWT_SECRET=replace_with_a_strong_secret
NODE_ENV=development

# Database (used by docker-compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vsmart?schema=public


# Web3 / IPFS
WEB3_STORAGE_TOKEN=

# Blockchain (optional)
MUMBAI_RPC_URL=
PRIVATE_KEY=

# AI service URL (optional). If empty, backend uses local heuristic (aiClient mock).
AI_SERVICE_URL=http://ai-service:8000/verify-image

# Mock settings
LOCAL_MOCK_MODE=true
